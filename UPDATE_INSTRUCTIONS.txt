GATEBALL LOVERS - NEXT UPDATE

1. Dashboard shows “Allow Chat Notifications” only while browser notification permission is not chosen. Existing and new members can opt in.
2. Signup password visibly says minimum 8 characters and browser/backend validation enforces it.
3. Signup country field is a searchable dropdown and includes Hong Kong and Macau.
4. App-style Back behavior is added: one Back shows “Do you want to exit the app? Yes / No” instead of walking back through visited pages.

NOTE: Service-worker push handling is prepared. True notification delivery while the PWA is fully closed still requires VAPID keys plus a secure push-subscription store/server sender; the current build does not pretend that browser realtime alone can deliver closed-app notifications.
